# Processed data

No new preprocessing is claimed by the repository recovery. Add generated datasets here only with the source hash, transformation script, schema and split manifest. Keep raw inputs immutable and keep labels separate from predictions.
